# Copy this file to .env and fill in your real MongoDB Atlas connection string.
# Get it from Atlas: Database > Connect > Drivers > Node.js
# It looks like: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/pooja-arrangers?retryWrites=true&w=majority

MONGODB_URI=

PORT=5000
